"""Pure value objects for the rover safety platform. Stdlib-only.

Callers import from submodules directly (`app.domain.enums`,
`app.domain.motion`, etc.). This package intentionally exposes no
top-level re-exports.
"""
