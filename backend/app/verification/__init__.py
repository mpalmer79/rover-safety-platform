"""Verification and evidence layer: requirements registry, scenario
verifier, audits, evidence generation, traceability matrix.

Callers import from submodules directly (`app.verification.requirements`,
`app.verification.evidence`, etc.). This package intentionally exposes
no top-level re-exports.
"""
