# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.coverage

# Build outputs
build/
dist/

# Run artefacts (deterministic engine + ROS scenario suites write here)
runs/

# Live runtime local overrides (Phase 14)
# Per-runner, may contain host-specific paths or runner identifiers.
live-runtime/runner-profile.local.json
live-runtime/baselines/maturity-baseline.json
